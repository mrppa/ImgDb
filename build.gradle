plugins {
    id 'java'
}

allprojects {
    group = 'com.mrppa.imgdb'
    version = System.getenv("VERSION") ? System.getenv("VERSION") : '0.1-SNAPSHOT'
}

clean.doFirst {
    delete "${rootProject.projectDir}/release"
}